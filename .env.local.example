# .env.local.example - 環境変数のサンプルファイル
#
# このファイルは環境変数の設定例を示すサンプルファイルです。
# 実際の環境変数は .env.local ファイルに設定してください。
#
# セットアップ手順:
# 1. このファイルをコピーして .env.local を作成
#    cp .env.local.example .env.local
#
# 2. .env.local を開いて、実際の値を設定
#
# 3. .env.local は .gitignore に含まれているため、Gitにコミットされません
#
# 注意:
# - .env.local.example は Git にコミットしても問題ありません（実際の値は含まれていないため）
# - .env.local には機密情報を含めるため、絶対に Git にコミットしないでください

# ========================================
# Google Analytics 4 設定
# ========================================

# Google Analytics 測定ID
# 形式: G-XXXXXXXXXX
#
# 取得方法:
# 1. Google Analytics (https://analytics.google.com/) にログイン
# 2. 管理 → プロパティ → データストリーム → ウェブストリームを選択
# 3. 「測定ID」をコピー（G-で始まる10桁のID）
#
# 例: NEXT_PUBLIC_GA_MEASUREMENT_ID=G-ABC1234567
#
# 重要: NEXT_PUBLIC_ プレフィックスが必要です
# - このプレフィックスにより、クライアント側でアクセス可能になります
# - Static Export では、ビルド時に値が埋め込まれます
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# ========================================
# その他の環境変数（将来的に追加可能）
# ========================================

# 例: APIキー
# NEXT_PUBLIC_API_KEY=your_api_key_here

# 例: サイトURL（OGP画像などで使用）
# NEXT_PUBLIC_SITE_URL=https://yourdomain.com
